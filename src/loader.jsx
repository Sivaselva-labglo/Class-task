import React from 'react';
import ReactLoading from 'react-loading';
 
const Example = ({ type, color }) => (
    <ReactLoading type="bubbles" color='black' height={67} width={75} />
);
 
export default Example;